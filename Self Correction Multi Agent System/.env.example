# ───────────────────────────────────────────────────────
# Environment variables for the Self-Correcting SQL Agent
# Copy this file → .env and fill in your real values.
# ───────────────────────────────────────────────────────

# ── LLM ─────────────────────────────────────────────────
GROQ_API_KEY=gsk_...
# OPENAI_API_KEY=sk-...                # only if using OpenAI
# LLM_MODEL=llama-3.3-70b-versatile    # optional override
# LLM_TEMPERATURE=0                    # optional override

# ── Database connection strings ─────────────────────────
MYSQL_CONNECTION_STRING=mysql+pymysql://user:password@localhost/dbname
# POSTGRESQL_CONNECTION_STRING=postgresql+psycopg2://user:password@localhost/dbname
# SQLITE_CONNECTION_STRING=sqlite:///./database.db
# SQLSERVER_CONNECTION_STRING=mssql+pyodbc://user:password@localhost/dbname?driver=ODBC+Driver+17+for+SQL+Server
# ORACLE_CONNECTION_STRING=oracle+cx_oracle://user:password@localhost:1521/dbname
